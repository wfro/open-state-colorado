<div class="notifications-container">
  <div class="notifications-header">Here are your representatives from the Colorado General Assembly!</div>
    <div class="row user-leg">
      <div class="col-md-6 user-legislators-left">
        <%= link_to(image_tag(@user.legislators.first.photo_url, class: "legislator-photos"), legislator_path(@user.legislators.first))%>
        </br>
        <%= @user.legislators.first.full_name %>
      </div> <div class="col-md-6 user-legislators-right">
        <%= link_to(image_tag(@user.legislators.last.photo_url, class: "legislator-photos"), legislator_path(@user.legislators.last))%>
        </br>
        <%= "#{@user.legislators.last.full_name}"%>
      </div>
    </div>
    <% unless @user.receives_notifications %>
      <div class="notifications-opt-in"><%= link_to "Click here to opt-in and recieve your first email notification!", notifications_opt_in_user_path %></div>
    <% else %>
      <p>Currently you are scheduled to receive updates twice a month.</p>
      <p>Opt-out at any point by clicking <%= link_to "here", notifications_opt_out_user_path %></p>
    <% end %>      
</div>
