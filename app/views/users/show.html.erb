<div class="user-profile">
  <p class="initial-user-info">Enter your address and email to get started with notifications!</p>

  <div class="address-email-form">
    <% if @user.legislators.empty? %>
      <%= form_tag(geolocate_user_path(@user), method: "post") do %>
        <div class="address-field">
          <%= label_tag(:address, "Address:") %>
          <%= text_field_tag(:address) %>
          <%= submit_tag("submit") %>
        </div>
      <% end %>
    <% else %>
      <div class="form-success">Address added!</div>
    <% end %>
      <% if @user.email.nil? %>
      <%= form_for(@user) do |f| %>
        <div class="email-field">
          <%= f.label :email, "Email:" %>
          <%= f.text_field :email %>
          <%= f.submit "submit" %>
        </div>
      <% end %>
      <% else %>
        <div class="form-success">Email added!</div>
      <% end %>
    </div>

    <div class="proceed-notifications">
      <p><%= link_to "Click here to manage your notifications!", notifications_user_path(@user), class: "proceed-link" %></p>
    </div>
  </div>
</div>
